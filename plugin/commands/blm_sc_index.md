---
description: index โปรเจ็คนี้เข้า SocratiCode ผ่าน blm (ไม่ต้องเปิด session ที่นั่น) รอจนจบแล้วรายงาน
argument-hint: [ไม่มี]
---

เรียก `blm_sc_index {}` แล้วพิมพ์ `result` ตามตัวอักษร — blm รอจน index จบเอง (อาจหลายนาที) · จบแล้วบอกเจ้าของบรรทัดเดียว: จำนวนไฟล์/chunk และว่า `blm_search` / `/blm_sc_graph` พร้อมใช้ · ล้ม (stderr ใน error) = รายงานตามนั้น ห้าม retry
