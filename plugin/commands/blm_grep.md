---
description: ค้นหาคำศัพท์ในไฟล์ แสดงผลเป็น array ของ hit พร้อม path, บรรทัด, snippet, text — ผู้ใช้เรียกตรงได้ด้วย `blm grep` ใน terminal
---

เรียก `blm_grep` แล้วพิมพ์ `hits` ที่ได้ออกมาเป็นตาราง term | path:line | start-end | lines | text — ห้ามสรุปเพิ่ม
