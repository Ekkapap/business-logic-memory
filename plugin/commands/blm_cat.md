# blm cat

Displays saved grep result file content with surrounding context lines.

Invokes the MCP tool `blm_cat` which reads a saved grep result JSON file, displays selected hits with configurable context, and shows line numbers from the original files.

The terminal output is formatted as a table with result IDs, file paths, line numbers, and code snippets.
